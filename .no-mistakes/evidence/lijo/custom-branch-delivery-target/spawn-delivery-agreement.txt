############ S1: brief scaffolded with --base rel/2026.9, spawn passes the same
   brief contract line: Delivery contract: mode=no-mistakes base=rel/2026.9
$ fm-spawn.sh s1 <project> codex --mode no-mistakes --yolo off --base rel/2026.9
spawned s1 harness=codex kind=ship mode=no-mistakes yolo=off base=rel/2026.9 window=firstmate:fm-s1 worktree=/tmp/fm-live-78928/spawn-current/wt
   (exit 0)
   state/s1.meta:
     project=/tmp/fm-live-78928/spawn-current/project
     kind=ship
     mode=no-mistakes
     yolo=off
     base=rel/2026.9

############ S2: same brief, spawn passes a DIFFERENT --base
$ fm-spawn.sh s2 <project> codex --mode no-mistakes --yolo off --base feat/other
error: delivery mismatch for s2: the brief says base=rel/2026.9 but this spawn passed base=feat/other; correct the flag or re-scaffold the brief so the branch the worker builds on and the branch this task is recorded to land on agree
   (exit 1)
   state/s2.meta: (absent - nothing was recorded)

############ S3: brief records a base, spawn omits --base (would land on main)
$ fm-spawn.sh s3 <project> codex --mode no-mistakes --yolo off
error: delivery mismatch for s3: the brief says base=rel/2026.9 but this spawn passed base=<the repo default branch>; correct the flag or re-scaffold the brief so the branch the worker builds on and the branch this task is recorded to land on agree
   (exit 1)
   state/s3.meta: (absent - nothing was recorded)

############ S4: brief records NO base, spawn passes --base
$ fm-spawn.sh s4 <project> codex --mode no-mistakes --yolo off --base rel/2026.9
error: delivery mismatch for s4: the brief says base=<the repo default branch> but this spawn passed base=rel/2026.9; correct the flag or re-scaffold the brief so the branch the worker builds on and the branch this task is recorded to land on agree
   (exit 1)
   state/s4.meta: (absent - nothing was recorded)

############ S5: no --base anywhere -> no base= key at all
$ fm-spawn.sh s5 <project> codex --mode no-mistakes --yolo off
spawned s5 harness=codex kind=ship mode=no-mistakes yolo=off window=firstmate:fm-s5 worktree=/tmp/fm-live-78928/spawn-current/wt
   (exit 0)
   state/s5.meta:
     project=/tmp/fm-live-78928/spawn-current/project
     kind=ship
     mode=no-mistakes
     yolo=off

############ S6 (adversarial): the brief's hand-written prose carries a
#   'Delivery contract: ' line ABOVE the generated one. The spawn disagrees on mode.
   prose line : 6:Delivery contract: land this on the release branch per the captain.
   real line  : 80:Delivery contract: mode=no-mistakes base=rel/2026.9
$ fm-spawn.sh s6 <project> codex --mode direct-PR --yolo off --base rel/2026.9
error: delivery mismatch for s6: the brief says mode=no-mistakes but this spawn passed --mode direct-PR; correct the flag or re-scaffold the brief so the worker's instructions and the task record agree
   (exit 1)
   state/s6.meta: (absent - nothing was recorded)

############ S7 (adversarial): same shadowing prose, spawn disagrees on BASE
$ fm-spawn.sh s7 <project> codex --mode no-mistakes --yolo off --base feat/other
error: delivery mismatch for s7: the brief says base=rel/2026.9 but this spawn passed base=feat/other; correct the flag or re-scaffold the brief so the branch the worker builds on and the branch this task is recorded to land on agree
   (exit 1)
   state/s7.meta: (absent - nothing was recorded)

############ S8: --relaunch refuses --base (the recorded contract is reused)
$ fm-spawn.sh s1 --relaunch --base feat/other
error: --relaunch reuses the task's recorded delivery target branch; --base cannot override it
   (exit 1)

############ S9: --base refused on a scout spawn
$ fm-spawn.sh s9 <project> codex --scout --base rel/2026.9
error: --base applies only to ship spawns; a scout delivers a report and a secondmate records its own fixed posture
   (exit 1)

############ S10: a batch spawn carries one shared --base to every pair,
#  and each pair still re-validates it against its own brief.
#  b1, b2 briefs record base=rel/2026.9; b3's brief records none.
$ fm-spawn.sh b1=<proj> b2=<proj> b3=<proj> --harness codex --mode no-mistakes --yolo off --base rel/2026.9
spawned b1 harness=codex kind=ship mode=no-mistakes yolo=off base=rel/2026.9 window=firstmate:fm-b1 worktree=/tmp/fm-live-78928/spawn-current/wt
spawned b2 harness=codex kind=ship mode=no-mistakes yolo=off base=rel/2026.9 window=firstmate:fm-b2 worktree=/tmp/fm-live-78928/spawn-current/wt
error: delivery mismatch for b3: the brief says base=<the repo default branch> but this spawn passed base=rel/2026.9; correct the flag or re-scaffold the brief so the branch the worker builds on and the branch this task is recorded to land on agree
batch: FAILED to spawn b3 (/tmp/fm-live-78928/spawn-current/project)
   (exit 1)
   state/b1.meta base= -> base=rel/2026.9
   state/b2.meta base= -> base=rel/2026.9
   state/b3.meta base= -> (no record)
