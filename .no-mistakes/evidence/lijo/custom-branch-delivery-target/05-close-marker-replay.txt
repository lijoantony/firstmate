### Step 1: cleanup is interrupted - tasks-axi cannot land the close
  exit=1
      error: r-pct's endpoint and local copy are cleaned up, but its backlog item could not be closed atomically (tasks-axi: simulated interruption during close); the pending close is recorded and the next session start retries it
  pending-close record:
      id=r-pct
      data=/private/tmp/fmlive/replay/r-pct/home/data
      spawn_gen=s1789700000.1.26548
      cleanup_incomplete=0
      arg=--note
      arg=local%20feat/50%stack

### Step 2: the next session start replays it with real tasks-axi
  exit=0
      BOOTSTRAP_INFO: closed the backlog item for r-pct that an interrupted cleanup left open
      MISSING: treehouse (install: curl -fsSL https://kunchenguid.github.io/treehouse/install.sh | sh)
      MISSING: gh-axi (install: npm install -g gh-axi && gh-axi setup hooks)
  backlog after replay:
      # Backlog
      
      ## In flight
      ## Queued
      ## Done
      - [x] r-pct - Stacked task r-pct (repo: project) (kind: ship) (done 2026-09-18)
        local feat/50%stack
PASS  the replayed note is exactly 'local feat/50%stack' - the % survived the round trip

########## adversarial: tampered pending-close records ##########
PASS  a note whose branch carries raw whitespace is refused and the item stays open
      backlog item still open; marker kept: tam1.backlog-close
PASS  a note whose branch starts with a dash is refused and the item stays open
      backlog item still open; marker kept: tam2.backlog-close
PASS  a note with no local%20 prefix at all is refused and the item stays open
      backlog item still open; marker kept: tam3.backlog-close
PASS  an over-long note is refused and the item stays open
      backlog item still open; marker kept: tam4.backlog-close

SUMMARY pass=4 fail=0
